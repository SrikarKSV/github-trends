import React, { Component } from 'react';

export default class Menu extends Component {
  render() {
    return (
      <div>
        <h2>Find the trending repositories or developers on GitHub!</h2>
      </div>
    );
  }
}
